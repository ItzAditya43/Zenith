fastapi==0.115.6
# mcp pulls in a newer starlette that breaks fastapi's pin; pinned here
# explicitly so a future `pip install` doesn't silently re-break it.
# (mcp also pulls sse-starlette for ITS server-side SSE transport, which
# we don't use — only the stdio client — so that conflict is harmless.)
starlette>=0.40.0,<0.42.0
uvicorn[standard]==0.34.0
mcp==1.28.1
httpx==0.28.1
# mcp requires pydantic>=2.11; bumped from 2.10.4 for that (fastapi 0.115.6
# is fine with anything <3.0.0, so this is a safe bump, not a downgrade risk).
pydantic==2.13.4
python-multipart==0.0.20
pypdf==5.1.0
python-docx==1.1.2
Pillow==11.1.0
faster-whisper==1.1.1
piper-tts==1.4.2
pyttsx3==2.98
aiofiles==24.1.0
python-magic==0.4.27
beautifulsoup4==4.12.3
lxml==5.3.0
youtube-transcript-api==1.2.4
# --- dev / test ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
sqlite-vec>=0.1.6
requests>=2.31
# Raw Chrome DevTools Protocol client for the browser_* agent tools — no
# Playwright/Selenium install (~300MB+ of browser binaries each); we drive
# the system chromium package directly over its own CDP websocket.
websockets>=12.0,<14.0
