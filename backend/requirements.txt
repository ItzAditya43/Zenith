fastapi==0.128.0
# On Python 3.14, mcp requires starlette>=0.48.0 (it only needs starlette
# >=0.27 on older Pythons). fastapi 0.115.x capped starlette below that, so
# `pip install` couldn't satisfy both — bumped fastapi to a version whose
# own starlette range (>=0.40,<0.51) overlaps mcp's, on every Python version.
starlette>=0.40.0,<0.51.0
uvicorn[standard]==0.34.0
mcp==1.28.1
httpx==0.28.1
# mcp requires pydantic>=2.11; bumped from 2.10.4 for that (fastapi 0.115.6
# is fine with anything <3.0.0, so this is a safe bump, not a downgrade risk).
pydantic==2.13.4
python-multipart==0.0.20
pypdf==5.1.0
python-docx==1.1.2
# Single-conversation PDF export — pure-Python, no system deps (no Cairo/
# Pango/headless browser), matching the stdlib-first philosophy.
fpdf2==2.8.8
Pillow==11.1.0
faster-whisper==1.1.1
piper-tts==1.4.2
pyttsx3==2.98
aiofiles==24.1.0
# Multi-device sync crypto (AES-GCM, scrypt, HKDF) — already pulled in
# transitively before this pin; pinned explicitly now that sync_service
# imports it directly.
cryptography>=42.0.0
python-magic==0.4.27
beautifulsoup4==4.15.0
lxml==5.3.0
youtube-transcript-api==1.2.4
sqlite-vec>=0.1.6
requests>=2.31
# Raw Chrome DevTools Protocol client for the browser_* agent tools — no
# Playwright/Selenium install (~300MB+ of browser binaries each); we drive
# the system chromium package directly over its own CDP websocket.
websockets>=12.0,<14.0
# /metrics Prometheus/OpenMetrics scrape endpoint — lightweight, no
# transitive dependency bloat.
prometheus-client==0.21.1
